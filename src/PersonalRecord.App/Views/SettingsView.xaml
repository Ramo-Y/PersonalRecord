<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PersonalRecord.Views.SettingsView"
             x:Name="SettingsViewXaml"
             Title="SettingsView"
             xmlns:viewModels="clr-namespace:PersonalRecord.Views"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material">

    <ContentPage.BindingContext>
        <viewModels:SettingsViewModel/>
    </ContentPage.BindingContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>

        <material:DataGrid Grid.Column="1" Grid.Row="1"
                           ItemsSource="{Binding UnitItems}"
                           HorizontalOptions="Center">
            <material:DataGrid.Columns>
                <material:DataGridColumn ValueBinding="{Binding Name}"
                                         Title="Name"/>
            </material:DataGrid.Columns>
        </material:DataGrid>
    </Grid>
</ContentPage>